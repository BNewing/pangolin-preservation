<%= form_with model: @gif, local: true do |form| %>
	<% if @gif.errors.any? %>
	    <div id="error_explanation">
	      <h2>There was an error</h2>
	      <p><%= pluralize(@gif.errors.count, "error") %> prohibited this gif from being saved:</p>
	      <ul>
	        <% @gif.errors.full_messages.each do |msg| %>
	          <li><%= msg %></li>
	        <% end %>
	      </ul>
	    </div>
	  <% end %>

	<p>
	 	<%= form.label :description %><br>
	 	<%= form.text_field :description %>
	</p>
 
  	<p>
    	<%= form.label :url %><br>
    	<%= form.text_area :url %>
 	</p>
 
  	<p>
    	<%= form.submit %>
  	</p>
<% end %>